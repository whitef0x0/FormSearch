// Generated by CoffeeScript 1.3.3
(function() {
  var results, source_results, template_results;

  source_results = $("#result-template").html();

  template_results = Handlebars.compile(source_results);

  String.prototype.trim = function() {
    return this.replace(/^\s+|\s+$/g, '');
  };

  results = {};

  $("#omnibox").keyup(function(e) {
    var context, html, key, out_results, result, score, term, terms, _i, _j, _len, _len1;
    key = e.keyCode || e.which;
    terms = $(this).val().trim().split(" ");
    out_results = [];
    score = 0;
    for (_i = 0, _len = results.length; _i < _len; _i++) {
      result = results[_i];
      result.score = 0;
      for (_j = 0, _len1 = terms.length; _j < _len1; _j++) {
        term = terms[_j];
        if (!term) {
          continue;
        }
        term = term.toLowerCase();
        if (result.name.toLowerCase().indexOf(term) > -1) {
          result.score += 1;
        }
        if (result.city.toLowerCase().indexOf(term) > -1) {
          result.score += 1;
        }
        if (result.title.toLowerCase().indexOf(term) > -1) {
          result.score += 1;
        }
      }
      if (result.score > 0) {
        console.log(result.score);
        out_results.push(result);
      }
    }
    out_results.sort(function(a, b) {
      return b.score - a.score;
    });
    context = {
      results: out_results
    };
    html = template_results(context);
    if ((out_results + ' ' === ' ' && $("#omnibox").val() !== '') && terms[0].length >= 2) {
      $(".results").html('<br> <i> <p class = "lead"> The search term(s) "' + terms + '" did not yield any results. <br> Check your spelling and try again </p></i>');
    } else {
      $(".results").empty();
      $(".results").html(html);
    }
    return $.get('/api/results', function(rsp) {
      return results = rsp;
    });
  });

}).call(this);
